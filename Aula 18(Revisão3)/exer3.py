# Você tem uma lista de valores que representa a temperatura máxima diária. Temperaturas: temperaturas = [28.5, 32.1, 25.9, 30.0, 27.8, 32.1] Crie uma função chamada encontrar_extremos que receba esta lista e retorne uma tupla contendo o valor mínimo e o valor máximo da lista, respectivamente. A função deve ser capaz de lidar com listas vazias, retornando (0.0, 0.0) neste caso.
# def encontrar_extremos(lista_temp):
#     max_temperatura = max(lista_temp)
#     min_valor = min(lista_temp)
#     if not lista_temp:
#         lista_temp.append(0.0, 0.0)
#     return (max_temperatura, min_valor)
# temperaturas = [28.5, 32.1, 25.9, 30.0, 27.8, 32.1]
# temp = encontrar_extremos(temperaturas)
# print (f"O valor maximo da temperatura{temp[0]}")
# print (f"O valor minimo da temperatura{temp[1]}")


print('-'*40)
